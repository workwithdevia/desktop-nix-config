{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "bashdb",
            "request": "launch",
            "name": "Depurar script Bash",
            "program": "${file}"
        }
    ]
}